export const CHARACTER_SIZE = 50;
export const JUMP_SPEED = 10;
export const JUMP_MAX_HEIGHT = 120; // this could be relative to char size

// Used for direction of travelling entities
export const LEFT = "LEFT";
export const RIGHT = "RIGHT";
